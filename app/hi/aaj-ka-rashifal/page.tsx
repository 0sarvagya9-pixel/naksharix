import type { Metadata } from "next";
import Link from "next/link";
import { MoonStar } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Section } from "@/components/section";
import { JsonLd } from "@/components/seo/json-ld";
import { SeoInternalLinks } from "@/components/seo/internal-links";
import { env } from "@/lib/env";
import { seo } from "@/lib/seo";

export const metadata: Metadata = seo({
  title: "?? ?? ?????? - Aaj Ka Rashifal Today | Naksharix",
  description: "Naksharix ?? ?? ?? ?????? ?????? ??? ?? ??? ?? ????? ??????, ?????, ?????, ??, ?????????, ??? ??? ?? ?????",
  path: "/hi/aaj-ka-rashifal",
  keywords: ["Aaj Ka Rashifal", "?? ?? ??????", "Hindi Horoscope", "Daily Horoscope Hindi"]
});

export default function AajKaRashifalPage() {
  const schema = { "@context": "https://schema.org", "@type": "WebPage", name: "?? ?? ??????", url: `${env.NEXT_PUBLIC_APP_URL}/hi/aaj-ka-rashifal`, inLanguage: "hi-IN" };
  return (
    <main className="star-field">
      <Section>
        <JsonLd id="aaj-ka-rashifal-schema" data={schema} />
        <p className="text-sm font-semibold uppercase tracking-[0.22em] text-[#FFD700]">Hindi Horoscope</p>
        <h1 className="mt-3 font-cinzel text-4xl font-black">?? ?? ??????</h1>
        <p className="mt-4 max-w-3xl naksh-muted-text">??? ?? ??? ?? ?? ?? ?????? ????? ?? ?????, ?????, ??, ?????????, ??? ??? ?? ?????? ?? ???? ?????</p>
        <Card className="glass mt-8">
          <CardContent className="grid gap-3 pt-6 sm:grid-cols-2 lg:grid-cols-3">
            {["????? ??????", "????? ??????", "?? ??????", "????????? ??????", "??? ???", "??? ???"].map((item) => (
              <div key={item} className="flex items-center gap-3 rounded-md border border-[#D4AF37]/20 p-3"><MoonStar className="h-4 w-4 text-[#FFD700]" />{item}</div>
            ))}
          </CardContent>
        </Card>
        <div className="mt-8 flex flex-wrap gap-3"><Button asChild><Link href="/horoscope/all-signs/today">All signs today</Link></Button><Button variant="outline" asChild><Link href="/hi/free-kundli">???? ??????</Link></Button></div>
        <SeoInternalLinks title="???????? Naksharix ??????" />
      </Section>
    </main>
  );
}
