import type { Metadata } from "next";
import Link from "next/link";
import { HeartHandshake } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Section } from "@/components/section";
import { JsonLd } from "@/components/seo/json-ld";
import { SeoInternalLinks } from "@/components/seo/internal-links";
import { env } from "@/lib/env";
import { seo } from "@/lib/seo";

export const metadata: Metadata = seo({
  title: "?????? ????? - Kundli Milan for Marriage | Naksharix",
  description: "Naksharix ?? ?????? ?????, ??? ?????, ??????? ???, ????? ???????? ?? ?????? ?? ????????? ??? ?????",
  path: "/hi/kundli-milan",
  keywords: ["Kundli Milan", "?????? ?????", "Gun Milan", "Marriage Matching"]
});

export default function KundliMilanHindiPage() {
  const schema = { "@context": "https://schema.org", "@type": "Service", name: "?????? ?????", url: `${env.NEXT_PUBLIC_APP_URL}/hi/kundli-milan`, inLanguage: "hi-IN", provider: { "@type": "Organization", name: "Naksharix" } };
  return (
    <main className="star-field">
      <Section>
        <JsonLd id="kundli-milan-hi-schema" data={schema} />
        <p className="text-sm font-semibold uppercase tracking-[0.22em] text-[#FFD700]">Marriage Astrology</p>
        <h1 className="mt-3 font-cinzel text-4xl font-black">?????? ?????</h1>
        <p className="mt-4 max-w-3xl naksh-muted-text">??? ?????, ??????? ???, ???????? ????? ?? ????? ?? ????? ????????? ???? ?? ??? ???? ??? ??????</p>
        <div className="mt-8 grid gap-4 md:grid-cols-3">
          {["??? ?????", "??????? ?????", "????? ????????"].map((item) => (
            <Card key={item} className="glass"><CardContent className="space-y-3 pt-6"><HeartHandshake className="h-5 w-5 text-[#FFD700]" /><h2 className="font-cinzel text-lg font-bold">{item}</h2><p className="text-sm naksh-muted-text">Naksharix ???? ???? ????? ?? ???? ?? ?????????? ????? ?????????? ???? ???</p></CardContent></Card>
          ))}
        </div>
        <div className="mt-8 flex flex-wrap gap-3"><Button asChild><Link href="/matchmaking">Start matching</Link></Button><Button variant="outline" asChild><Link href="/consultation">???????? ?? ????</Link></Button></div>
        <SeoInternalLinks title="?????? ?? ????? ???" />
      </Section>
    </main>
  );
}
