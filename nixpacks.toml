[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["prisma generate", "next build"]

[start]
cmd = "prisma migrate deploy && node .next/standalone/server.js"
